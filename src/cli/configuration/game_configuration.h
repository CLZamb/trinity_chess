#ifndef GAME_CONFIGURATION
#define GAME_CONFIGURATION

class GameConfiguration {
public:
  GameConfiguration() = default;
  // GameConfiguration(GameConfiguration  &&) = default;
  // GameConfiguration(const GameConfiguration  &) = default;
  // GameConfiguration&operator=(GameConfiguration  &&) = default;
  // GameConfiguration&operator=(const GameConfiguration  &) = default;
  virtual ~GameConfiguration() = default;

private:

};
#endif // !GAME_CONFIGURATION
